# Backend Scope

Use this scope for implementation contracts, technical documentation, and repository reality checks tied to Python backend work.

## Load First

- `docs/04.specs/README.md`
- `pyproject.toml`
- `.github/workflows/ci.yml`

## Task Constraints

- Treat `pyproject.toml` and the current directory tree as the runtime truth.
- Reference `TIL/` and `tests/` only when they are relevant to the active work.

## Verification

- Commands, paths, and tooling references must match the current repository state.
