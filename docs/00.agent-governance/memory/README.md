# Memory Notes

This directory is optional support for reusable governance memory.

## Purpose

- Store future memory templates or notes that help agents preserve stable repository knowledge.
- Keep runtime-critical rules out of this directory.

## Usage

- Use `template.md` as the starting point for any future reusable note.
- Keep memory files concise and topic-scoped.
